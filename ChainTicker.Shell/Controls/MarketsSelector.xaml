<UserControl
    x:Class="ChainTicker.Shell.Controls.MarketsSelector"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:models="clr-namespace:ChainTicker.Shell.Models"
    d:DataContext="{d:DesignInstance IsDesignTimeCreatable=False,
                                     Type={x:Type models:ExchangeModel}}"
    d:DesignHeight="300"
    d:DesignWidth="200"
    mc:Ignorable="d">

    <StackPanel>

        <TextBlock Margin="10,0,0,10" Text="{Binding Description}" />

        <ItemsControl
            HorizontalAlignment="Stretch"
            VerticalAlignment="Stretch"
            ItemsSource="{Binding Markets}">
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <StackPanel Orientation="Horizontal">
                        <CheckBox IsChecked="{Binding IsSubscribed}" />
                        <TextBlock Margin="10,0,0,0" Text="{Binding DisplayName}" />
                    </StackPanel>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>


    </StackPanel>

</UserControl>

