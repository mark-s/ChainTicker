<Window
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:prism="http://prismlibrary.com/"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:viewModels="clr-namespace:ChainTicker.Shell.ViewModels"
    xmlns:dd="urn:gong-wpf-dragdrop"
    x:Class="ChainTicker.Shell.Views.MainWindow"
    Title="ChainTicker"
    Width="776.127"
    Height="447"
    mc:Ignorable="d"
    d:DataContext="{d:DesignInstance IsDesignTimeCreatable=False, Type={x:Type viewModels:MainWindowViewModel}}"
    prism:ViewModelLocator.AutoWireViewModel="True">



    <Grid>

        <Button
            Width="191"
            Margin="26,10,0,0"
            HorizontalAlignment="Left"
            VerticalAlignment="Top"
            Command="{Binding Exchanges[0].GetMarketsCommand}"
            Content="Get Markets" />

        <ItemsControl
            Width="191"
            Height="332"
            Margin="26,35,0,0"
            HorizontalAlignment="Left"
            VerticalAlignment="Top"
            ItemsSource="{Binding Exchanges[0].Markets}">
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <StackPanel Orientation="Horizontal">
                        <CheckBox IsChecked="{Binding IsSubscribed}" />
                        <TextBlock Text="{Binding DisplayName}"  Margin="10,0,0,0"/>
                    </StackPanel>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>



        <ItemsControl
            Width="394"
            Height="18"
            Margin="244,193,0,0"
            HorizontalAlignment="Left"
            VerticalAlignment="Top"
            ItemsSource="{Binding Exchanges}"
            ItemTemplate="{StaticResource ExhangeWithMarketsTemplate}"

            >

            <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <StackPanel Orientation="Horizontal" />
                </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>

        </ItemsControl>

    </Grid>


</Window>







